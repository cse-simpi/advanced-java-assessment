server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: YOUR_GIT_REPO_URL

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka